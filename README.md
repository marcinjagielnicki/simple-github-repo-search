# simple-github-repo-search

To install project:
* Run: `make warmup-project`
* Run: `make front-install`
* Run: `make front-build`
* Fill your username and password into `.env.local` file

Project by default is accessible by `http://localhost:8081/`
